# Folder that keeps the BLOB files
resources.storagedir = resources
resources.filesdir = files

# this is the queue/address of the resource module
resources.address = Resources

# If the last access is longer ago than keepHours hours then the resource will be GC'ed
resources.keepHours = 1
